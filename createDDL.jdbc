CREATE TABLE Customer (id VARCHAR(255) NOT NULL, statusgroup VARCHAR(255), name VARCHAR(255), city VARCHAR(255), PRIMARY KEY (id))
CREATE TABLE TypeTest (id BIGINT NOT NULL, tdateVal DATE, tfloatVal FLOAT, tintVal INTEGER, tdoubleVal DOUBLE, tdecimalVal BIGINT, tdateTimeVal DATETIME, tbooleanVal TINYINT(1) default 0, tlongVal BIGINT, tstringVal VARCHAR(255), tshortVal SMALLINT, PRIMARY KEY (id))
CREATE TABLE order_table (id VARCHAR(255) NOT NULL, SHIPPING_ADDRESS VARCHAR(255), CUSTOMER_id VARCHAR(255), PRIMARY KEY (id))
ALTER TABLE order_table ADD CONSTRAINT FK_order_table_CUSTOMER_id FOREIGN KEY (CUSTOMER_id) REFERENCES Customer (id)
